class SourceCodeFile:

    def __init__(self, file_path, content_corpus, word_count,file_length):
        self.file_path = file_path
        self.content_corpus = content_corpus
        self.word_count = word_count
        self.file_lenght = file_length
